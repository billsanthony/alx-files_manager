// utils.js
const checkRedisStatus = () => {
  // Logic to check Redis status
  // Return true if Redis is alive, false otherwise
};

const checkDBStatus = () => {
  // Logic to check DB status
  // Return true if DB is alive, false otherwise
};

module.exports = { checkRedisStatus, checkDBStatus };
